#include <stdio.h>

int main () {

    char word[10];
    char *ptr = word;

    printf("enter word: ");
    scanf("%s",word);

    //*ptr = word;

    for (int i = 0; i < ptr[i]; i++)
    {
        printf("%c ",ptr[i]);
    }
    
    printf("\n");

    return 0;
}